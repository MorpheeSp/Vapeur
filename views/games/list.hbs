<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Vapeur - Jeux</title>
    <style>
        body { font-family: Arial, sans-serif; padding: 20px; }
        nav a { margin-right: 10px; }
        .game { background: #f0f0f0; padding: 15px; margin: 10px 0; }
    </style>
</head>
<body>
    <nav>
        <a href="/">Accueil</a>
        <a href="/games">Jeux</a>
        <a href="/genres">Genres</a>
        <a href="/editeurs">Éditeurs</a>
        <a href="/games/new">Ajouter un jeu</a>
        <a href="/editeurs/new">Ajouter un éditeur</a>
    </nav>

    <h1>Tous les jeux</h1>

    {{#if games}}
        {{#each games}}
        <div class="game">
            <strong><a href="/games/{{id}}">{{titre}}</a></strong>
            {{#if releaseDateText}}<p>Sortie : {{releaseDateText}}</p>{{/if}}
            <p>{{description}}</p>
            {{#if genre}}
                <p>Genre : <a href="/genres/{{genre.id}}">{{genre.titreGenre}}</a></p>
            {{/if}}
            {{#if editeur}}
                <p>Éditeur : <a href="/editeurs/{{editeur.id}}">{{editeur.nom}}</a></p>
            {{/if}}
            {{#if isFeatured}}<p>Sur la page d'accueil</p>{{/if}}
            <a href="/games/{{id}}/edit">Modifier</a>
            <form action="/games/{{id}}/delete" method="POST" style="display: inline;">
                <button type="submit">Supprimer</button>
            </form>
        </div>
        {{/each}}
    {{else}}
        <p>Aucun jeu enregistré.</p>
    {{/if}}
</body>
</html>

